"use client";
import Image from "next/image";
import { AnimatePresence, motion } from "framer-motion";
import { X } from "lucide-react";
import { useState } from "react";
import { certifications, type Certification } from "@/data/certifications";
export default function Certifications(){const [selected,setSelected]=useState<Certification|null>(null);return <section className="section section-tint"><p className="eyebrow">Credentials</p><h2 className="section-title mt-4">Continually sharpening my craft.</h2><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{certifications.map(c=><motion.button whileHover={{y:-4}} onClick={()=>setSelected(c)} key={c.name} className="card text-left"><div className="relative h-32 overflow-hidden rounded-xl bg-moss/10"><Image src={c.image} alt={`${c.name} certificate`} fill className="object-cover"/></div><h3 className="mt-5 font-semibold text-ink dark:text-white">{c.name}</h3><p className="mt-1 text-sm text-ink/60 dark:text-white/60">{c.issuer} · {c.date}</p></motion.button>)}</div><AnimatePresence>{selected&&<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={()=>setSelected(null)} className="fixed inset-0 z-50 grid place-items-center bg-ink/70 p-5"><motion.div initial={{scale:.96}} animate={{scale:1}} onClick={e=>e.stopPropagation()} className="relative w-full max-w-3xl rounded-2xl bg-cream p-3 dark:bg-[#1b2922]"><button aria-label="Close certificate preview" onClick={()=>setSelected(null)} className="icon-button absolute right-5 top-5 z-10 bg-cream/80 dark:bg-ink/80"><X size={18}/></button><div className="relative aspect-[1.6] overflow-hidden rounded-xl"><Image src={selected.image} alt={selected.name} fill className="object-cover"/></div></motion.div></motion.div>}</AnimatePresence></section>}
