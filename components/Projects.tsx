"use client";
import { useState } from "react";
import { motion } from "framer-motion";
import { ExternalLink, Github } from "lucide-react";
import { projects, type Project } from "@/data/projects";
import ProjectModal from "./ProjectModal";
export default function Projects() { const [selected, setSelected] = useState<Project | null>(null); return <section id="projects" className="section"><p className="eyebrow">Selected work</p><h2 className="section-title mt-4">Projects with a purpose.</h2><div className="mt-10 grid gap-5 md:grid-cols-2">{projects.map((project,i)=><motion.article initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:i*.08}} whileHover={{y:-5}} onClick={()=>setSelected(project)} key={project.name} className="card group cursor-pointer"><div className="flex items-start justify-between"><span className="text-xs font-semibold uppercase tracking-[.16em] text-moss dark:text-lime">0{i+1}</span><ExternalLink size={18} className="text-ink/35 transition group-hover:text-moss dark:text-white/35"/></div><h3 className="mt-11 text-2xl font-semibold text-ink dark:text-white">{project.name}</h3><p className="mt-3 min-h-12 leading-6 text-ink/65 dark:text-white/65">{project.description}</p><div className="mt-6 flex flex-wrap gap-2">{project.stack.slice(0,5).map(x=><span key={x} className="tag">{x}</span>)}</div><div onClick={e=>e.stopPropagation()} className="mt-7 flex gap-4"><a className="project-link" target="_blank" href={project.github}><Github size={16}/>GitHub</a>{project.demo && <a className="project-link" target="_blank" href={project.demo}><ExternalLink size={16}/>Live Demo</a>}</div></motion.article>)}</div><ProjectModal project={selected} close={()=>setSelected(null)}/></section>; }
