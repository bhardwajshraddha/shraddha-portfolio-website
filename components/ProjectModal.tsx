"use client";
import { AnimatePresence, motion } from "framer-motion";
import { ExternalLink, Github, X } from "lucide-react";
import type { Project } from "@/data/projects";
export default function ProjectModal({ project, close }: { project: Project | null; close: () => void }) { return <AnimatePresence>{project && <motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={close} className="fixed inset-0 z-50 grid place-items-center bg-ink/60 p-4 backdrop-blur-sm"><motion.div initial={{opacity:0,y:20,scale:.98}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:20}} onClick={e=>e.stopPropagation()} className="max-h-[90vh] w-full max-w-3xl overflow-y-auto rounded-3xl bg-cream p-6 shadow-2xl dark:bg-[#1b2922] sm:p-8"><div className="flex items-start justify-between gap-4"><div><p className="eyebrow">Project case study</p><h2 className="mt-2 text-3xl font-semibold text-ink dark:text-white">{project.name}</h2></div><button aria-label="Close project details" onClick={close} className="icon-button"><X size={19}/></button></div><div className="mt-7 grid gap-6 md:grid-cols-2"><Detail title="The problem" text={project.problem}/><Detail title="The solution" text={project.solution}/></div><div className="mt-7"><h3 className="font-semibold text-ink dark:text-white">Highlights</h3><ul className="mt-3 grid gap-2 sm:grid-cols-2">{project.features.map(x=><li className="text-sm text-ink/70 dark:text-white/70" key={x}>✦ {x}</li>)}</ul></div><div className="mt-7"><h3 className="font-semibold text-ink dark:text-white">Tech stack</h3><div className="mt-3 flex flex-wrap gap-2">{project.stack.map(x=><span className="tag" key={x}>{x}</span>)}</div></div><div className="mt-8 flex gap-3"><a className="button-primary" target="_blank" href={project.github}><Github size={16}/>GitHub</a>{project.demo && <a className="button-secondary" target="_blank" href={project.demo}><ExternalLink size={16}/>Live Demo</a>}</div></motion.div></motion.div>}</AnimatePresence> }
function Detail({title,text}:{title:string;text:string}) { return <div><h3 className="font-semibold text-ink dark:text-white">{title}</h3><p className="mt-2 text-sm leading-6 text-ink/70 dark:text-white/70">{text}</p></div> }
